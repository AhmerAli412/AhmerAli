# Optional: GitHub contribution graph (GraphQL). Create a fine-grained PAT with read-only user scope.
# Add these in Vercel → Project → Settings → Environment Variables (same project keeps your domain).
GITHUB_TOKEN=
GITHUB_LOGIN=AhmerAli412

# Canonical site URL for Open Graph / metadata (defaults to https://ahmer.vercel.app if unset).
NEXT_PUBLIC_SITE_URL=https://ahmer.vercel.app
